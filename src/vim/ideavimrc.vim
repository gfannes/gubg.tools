map -r O#include ""<Esc>i
map -R O#include <><Esc>i
map -t otemplate <typename ><Esc>i
map -s onamespace {<Esc>o<Esc>khi

map -n o{<Enter>MSS_BEGIN(bool);<Enter>MSS_END();<Esc>k$

map -m jOMSS();<Esc>hi
